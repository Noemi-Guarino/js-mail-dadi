// Mail
// -Chiedi all'utente la sua email,
// -creare una lista tramite js array
// -controlla che sia nella lista di chi può accedere,
// -stampa un messaggio appropriato sull'esito del controllo.

 let correctemail = prompt("Inserisci la tua e-mail");
 console.log(correctemail);
 
 const emailList = [
 ];

 console.log(`guarino.noemi96@gmail.com` == values[5]);



// document.getElementById("searchemail").innerHTML =
// "hai inserito corettamente la tua email:" + email;